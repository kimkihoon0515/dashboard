<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.humintecTest.dashboard.dao.ScannerListDao">

    <insert id="insertScannerList">
        insert into scaner_per_slide(scanner_type,slide_sum) values (#{scanner_type},#{slide_sum})
    </insert>
</mapper>